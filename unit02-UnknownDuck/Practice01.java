import java.io.File;
import java.io.FileReader;
import java.io.IOException;
import java.sql.Array;
public class Practice01 {

    public static String arrayToString(int arr[]){
        String stringArr = int arr[];

        return stringArr;

    }

    public static printLines(String filename, String lowerLetter) throws IOException{
        File file = new File(" .gitignore");
        // I know I have to create a for loop that checks the letter and if it matches print it
        // while also having a counter that returns the total amount of times it printed 
        // I'm struggling to get this down in code on java however

        FileReader fileReader = 
            new FileReader("a_file.txt");
        BufferedReader reader =
            new BufferedReader(fileReader);
        String line = reader.readLine();
        reader.close();
        fileReader.close();

    public static void main(String[] args){
        
    }
    
}
